[project]
name = "frontend"
version = "0.1.0"
description = "Second mind frontend chat"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
frontend = "frontend:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
